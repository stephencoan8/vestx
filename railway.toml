[build]
builder = "nixpacks"

[deploy]
# Must run under sh so ${PORT} expands (Railway does not expand $PORT in bare exec form).
startCommand = "sh -c 'gunicorn main:app -k gthread -w 1 --threads 8 --timeout 120 --graceful-timeout 30 --bind 0.0.0.0:${PORT:-5000}'"
